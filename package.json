{
  "name": "perfume-collection-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm.cmd --prefix frontend run dev",
    "api": "npm.cmd --prefix backend run dev",
    "frontend": "npm.cmd --prefix frontend run dev",
    "backend": "npm.cmd --prefix backend run dev",
    "seed": "npm.cmd --prefix backend run seed",
    "build": "npm.cmd --prefix frontend run build",
    "install:all": "npm.cmd --prefix frontend install && npm.cmd --prefix backend install"
  }
}
